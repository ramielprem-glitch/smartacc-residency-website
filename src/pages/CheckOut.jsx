import PlaceholderPage from './PlaceholderPage'

export default function CheckOut() {
  return (
    <PlaceholderPage
      title="Check-Out"
      subtitle="Process guest departures and finalize billing"
      description="The check-out workflow will let staff review a guest's folio, settle outstanding charges, generate a final invoice, and release the room for housekeeping. Placeholder for now."
    />
  )
}
